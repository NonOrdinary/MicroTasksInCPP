// it was wasy basic DSA
/**
 * Create a program that takes a vector<string> and does all of this:
    prints all strings
    finds the longest string
    finds the shortest string
    counts how many strings contain a given character
    converts all strings to uppercase in a new vector
    removes empty strings
 */